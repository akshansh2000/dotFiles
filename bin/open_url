#!/usr/bin/env bash

history_file="/home/akshansh2000/.config/url_history"
update_history "$history_file"

query=`cat "$history_file" | dmenu -i -p "enter search query or url"`
[[ "$query" = "" ]] || qutebrowser --target=window "$query" &

update_history "$query" "$history_file"
